from models.calculo import Calculo

# calc: Calculo = Calculo(dificuldade=1)   # dificuldade como parâmetro padrão - barrado pelo / no construtor
# calc: Calculo = Calculo(1)  # dificuldade como parâmetro posicional
calc = Calculo(1)

print(calc)

