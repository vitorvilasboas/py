# Pycharm
# /.idea    guarda arquivos de configuração do projeto (criado automaticamente)
# /venv     guarda arquivos dos ambientes virtuais do projeto (criado automaticamente)

# Barra Inferior: run (saída padrão), Terminal, Console; Go to line; Interpreter
# Settings: View Toolbars; Themes; Color Skems; Fontsize; Ctrl+Mouse; File Encoding 'utf-8'
# Plugins


"""
Hotkeys:
    shift + F10          executa script completo (Run)
    alt + shift + e      executa seleção (Console)
    alt + 1              abre/fecha view lateral Project
    alt + 7              abre/fecha view lateral Structure
    alt + 4              abre/fecha toolbar inferior
    shift + esc          fecha a view atual (foco)
    ctrl + /             comentar bloco/linha
"""